<style lang="scss" is:global>
  @use "../styles/mixins/breakpoints" as *;

  .container {
    margin: 0 1rem;

    @include from("medium") {
      margin: 0 2rem;
    }

    @include from("large") {
      margin: 0 5rem;
    }

    @include from("extra-extra-large") {
      margin: 0 auto;
      width: 85rem;
    }
  }
</style>
