<style lang="scss" is:global>
  @use '../styles/mixins/breakpoints' as *;

  .container {
    padding: 0 1rem;
    width: 100vw;

    @include from('medium') {
      padding: 0 2rem;
    }

    @include from('large') {
      padding: 0 5rem;
    }

    @include from('extra-extra-large') {
      margin: 0 auto;
      padding: 0;
      width: 85rem;
    }
  }
</style>
